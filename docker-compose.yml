services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - chromadb_data:/data
    environment:
      - ANONYMIZED_TELEMETRY=FALSE

volumes:
  chromadb_data:
